# =================================================
# HydroIA Velian — Variables de entorno
# =================================================
# Renombra este archivo a ".env" (sin el .template)
# Pega tus llaves reales después del =
# NUNCA subas el archivo .env a GitHub

# Token del bot que te dio @BotFather
TELEGRAM_BOT_TOKEN=AQUÍ_PEGA_TU_TOKEN

# Llave API de Anthropic Console
ANTHROPIC_API_KEY=sk-ant-AQUÍ_PEGA_TU_LLAVE

# URL de tu proyecto Supabase (Settings → API → Project URL)
SUPABASE_URL=https://AQUI_TU_PROYECTO.supabase.co

# Service Role key de Supabase (Settings → API → service_role key)
# IMPORTANTE: usa la service_role, NO la anon, para el bot
SUPABASE_KEY=eyJhbGc.AQUI_TU_LLAVE_SERVICE_ROLE

# Puerto del servidor HTTP (para endpoint del sensor)
PORT=3001

# Modo de operación (development / production)
NODE_ENV=development
